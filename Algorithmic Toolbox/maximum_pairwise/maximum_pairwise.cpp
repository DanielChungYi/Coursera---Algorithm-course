#include<vector>
#include<iostream>
using namespace std;

long maximum_pairwise(const vector<long> input)
{
   int n = input.size();
   long max_1_num = -1;
    for(int i = 0; i < n ; i++)
	{
	   if(vector[i]>max_1) max_1 = i;
	   
	}
}
